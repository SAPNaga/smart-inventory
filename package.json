{
  "name": "smart-inventory",
  "version": "1.0.0",
  "description": "Tiny CAP + MCP demo — a books inventory accessible to AI",
  "scripts": {
    "start": "cds-serve"
  },
  "dependencies": {
    "@sap/cds": "^9",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": { "url": "db/books.db" }
      }
    }
  }
}
